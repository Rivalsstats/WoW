"""Social-media post generation for MythiStone.

Text generation (LLM social captions in llm.py, static blog/title copy in
static_copy.py, site links in links.py), the per-post-type wrappers around the
image_generation renderers (posts.py) and the selection/persistence pipeline +
CLI (pipeline.py). Image rendering itself lives in the image_generation package.
"""
